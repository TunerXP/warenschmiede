# Warenschmiede Webseite 2.0 – v33 Sticky Header Test

Änderung gegenüber v32:

- Header/Menü bleibt beim Scrollen dauerhaft oben sichtbar.
- Umsetzung zentral über `assets/js/ws-layout.js` + `assets/css/styles.css`.
- Keine einzelnen HTML-Menüs angepasst.
- Body bekommt automatisch die Klasse `ws-fixed-topbar`, damit der Seiteninhalt nicht vom fixierten Header verdeckt wird.

Kopieren: kompletten Inhalt dieses Ordners nach `webseite_2_0/` übernehmen und vorhandene Dateien ersetzen.
